import 'package:flutter/material.dart';

class HomePage extends StatelessWidget {
  final Function(int) onNavigate;

  const HomePage({super.key, required this.onNavigate});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: ElevatedButton(
        onPressed: () => onNavigate(1), // go to Profile
        child: const Text("Go to Profile"),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class ProfilePage extends StatelessWidget {
  final Function(int) onNavigate;

  const ProfilePage({super.key, required this.onNavigate});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: ElevatedButton(
        onPressed: () => onNavigate(2), // go to Message
        child: const Text("Go to Messages"),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class MessagePage extends StatelessWidget {
  final Function(int) onNavigate;

  const MessagePage({super.key, required this.onNavigate});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: ElevatedButton(
        onPressed: () => onNavigate(3), // go to Settings
        child: const Text("Go to Settings"),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class SettingsPage extends StatelessWidget {
  final Function(int) onNavigate;

  const SettingsPage({super.key, required this.onNavigate});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: ElevatedButton(
        onPressed: () => onNavigate(0), // back to Home
        child: const Text("Back to Home"),
      ),
    );
  }
}
